# Cloud VS Code Workstation Source (`src/`)

Source implementation for the Cloud VS Code workstation micro-app, including session locking (`session-coordinator.ts`), path traversal sanitization (`git-sanitizer.ts`), network audit logs (`network-auditor.ts`), and VS Code daemon lifecycle management (`vscode-bridge.ts`).
