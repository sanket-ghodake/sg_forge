# Dedicated Turso Database Client (`forge-apps/code/src/db/`)

Isolated Turso libSQL SQLite database instance (`code.db`) storing projects, user memberships, and active workstation claims in WAL mode.
