---
title: LLR-SUB-006 - PostMessage Sandboxed Frame Handshake & Origin Validation
description: Low-level requirement establishing bi-directional communication bridge between Portal and micro-apps.
---

import TraceabilityBadge from '../../../components/TraceabilityBadge.astro';

<div style="margin-bottom: 1.5rem; display: flex; gap: 0.5rem; align-items: center; flex-wrap: wrap;">
  <TraceabilityBadge reqId="LLR-SUB-006" type="LLR" status="verified" />
  <TraceabilityBadge reqId="HLR-PORTAL-202" type="HLR" status="verified" />
</div>

## 1. Algorithmic Specification

1. Listen for 'message' event on window object
2. Assert event.origin strictly matches approved internal service origin list
3. Parse event.data conforming to ForgePostMessage protocol: `{ type, payload, messageId }`
4. Verify message signature or token if message invokes privileged platform actions
5. Dispatch internal event handler and reply with acknowledgment message

## 2. Safety & Verification Invariants

* **PostMessage messages from unapproved origins must be silently dropped without exception**
* **Wildcard targetOrigin ('*') is strictly forbidden for privileged platform messages**

## 3. Bidirectional Traceability

* **Parent High-Level Requirement**: <TraceabilityBadge reqId="HLR-PORTAL-202" type="HLR" status="verified" />
* **Implemented in Source**: `apps/src/portal/src/server.ts`
* **Verified in Test Suite**: `apps/src/portal/test/security/iframe-sandbox.test.ts`
