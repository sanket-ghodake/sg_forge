---
title: Portal SPA Engine, App Launcher & Iframe Sandboxing
description: Architectural specifications for client-side view routing, new-tab app launching with noopener security, instantaneous DOM hydration, and sandboxed micro-app iframes.
---

import DiagramEmbed from '../../../components/DiagramEmbed.astro';
import ExecutiveCard from '../../../components/ExecutiveCard.astro';
import TraceabilityBadge from '../../../components/TraceabilityBadge.astro';

<ExecutiveCard 
  title="Zero-Refresh Single Page Application Architecture & Secure App Launcher"
  analogy="The SG Forge Portal functions like a mission-control dashboard: switching between views happens instantly without reloading, and launching any Forge App opens it in a dedicated, isolated browser tab — the mission-control panel stays live and unaffected."
  impact="Guarantees fluid responsiveness down to 320px mobile viewports, sub-16ms view transitions, complete iframe security sandboxing, and zero Portal state loss when users open Forge Apps."
/>

<TraceabilityBadge reqId="SR-UI-001" type="SR" status="verified" />
<TraceabilityBadge reqId="HLR-PORTAL-201" type="HLR" status="verified" />
<TraceabilityBadge reqId="HLR-PORTAL-202" type="HLR" status="verified" />
<TraceabilityBadge reqId="LLR-SUB-006" type="LLR" status="verified" />
<TraceabilityBadge reqId="LLR-PORTAL-002" type="LLR" status="verified" />

## 1. Portal Architecture & Navigation Lifecycle

The SG Forge Portal (`@forge/portal`) strictly operates as a Single Page Application (SPA). Hard page refreshes during navigation are strictly forbidden by architectural directive 15.

<DiagramEmbed 
  diagram="portal-spa" 
  title="Portal Single Page Application (SPA) View Routing Engine" 
  caption="Client-side history navigation, instantaneous DOM hydration, and secure cross-origin postMessage bridging."
/>

## 2. Technical Specifications

### Client-Side View Dispatcher
* **History State Integration**: Navigation clicks intercept standard anchor events, pushing route states via `history.pushState(null, '', path)` and dispatching internal view hydration events.
* **Instantaneous View Transitions**: Views are rendered client-side using lightweight DOM templates without round-trip HTML page reloads.
* **Responsive Layout Engine**: Viewports adjust dynamically across Desktop (>1200px), Tablet (768px - 1199px), and Mobile (320px - 767px) with zero horizontal scrollbars.

### Iframe Sandboxing & Security Boundaries
When rendering autonomous Forge submodules inside the Portal:
* Micro-apps are loaded inside isolated `<iframe>` elements configured with strict sandbox attributes:
  ```html
  <iframe 
    src="/apps/code/" 
    sandbox="allow-scripts allow-same-origin allow-forms"
    referrerpolicy="strict-origin-when-cross-origin"
    class="forge-submodule-frame">
  </iframe>
  ```
* **PostMessage RPC Bridge**: Cross-origin communication between the Portal shell and embedded Forge apps uses cryptographically validated JSON payloads:
  ```typescript
  window.addEventListener('message', (event) => {
    if (event.origin !== expectedOrigin) return;
    handleSubmoduleEvent(event.data);
  });
  ```

## 3. New-Tab App Launcher & Portal State Isolation

When a user clicks "Launch" or any app card from the Portal's My Apps, Pinned Favorites, Marketplace, or Command Palette, the Forge App is opened in an **isolated new browser tab**:

* Every app launch anchor carries `target="_blank" rel="noopener noreferrer"` — preventing the launched Forge App tab from accessing the Portal's `window.opener`, suppressing the `Referer` header, and ensuring the Portal's SPA state (active view, scroll position, open modals) is fully preserved.
* Command palette search results that trigger app navigation use `window.open(ingressPath, '_blank', 'noopener,noreferrer')` for equivalent security without an anchor element.
* App ingress paths are always sourced from the authenticated app registry API and HTML-escaped before injection to prevent XSS.

**Portal state after launching an app:**

| User Action | Portal Tab | Forge App Tab |
|---|---|---|
| Click "Launch" on any app card | Remains on current view | Opens in new dedicated tab |
| Log out from Portal | Redirects to login | Receives `BroadcastChannel` LOGOUT signal instantly |
| Session expires (inactivity) | Redirects with `?return_url` | Redirects with `?return_url` |

<TraceabilityBadge reqId="LLR-PORTAL-002" type="LLR" status="verified" />

## 4. End-to-End Enterprise User Journey

From initial authentication to multi-tenant workspace interactions, every interaction runs client-side with zero hard page refreshes:

<DiagramEmbed 
  diagram="user-journey" 
  title="End-to-End Enterprise User Journey & Org Workspace" 
  caption="Zero-trust identity authentication, single page application hydration, multi-tenant org switching, new-tab Forge App launch, and cross-tab logout synchronization."
/>
