# Tier 4: Contract Tests - @forge/docs

Contract tests validating API response schemas in `@forge/docs`:
- `/health` probe JSON contract (status, service name, port, uptime, timestamp)
- `/ready` probe JSON contract (ready flag, staticDirReady)
