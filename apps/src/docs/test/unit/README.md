# Tier 1: Unit Tests - @forge/docs

Unit tests for isolated pure functions in `@forge/docs`:
- MIME type resolution (`getMimeType`)
- Safe file path normalization (`resolveSafeFilePath`)
- Configuration loader (`loadDocsConfig`)
